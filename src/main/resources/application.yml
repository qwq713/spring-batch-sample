spring:
  application:
    name: opt-dash-batch
  datasource:
#    url: jdbc:h2:mem:testdb
#    driver-class-name: org.h2.Driver
#    username: sa
#    password: password
    url: jdbc:mariadb://localhost:3306/spring
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: root
#  h2:
#    console:
#      enabled: true
#      path: /h2-console

  batch:
    job:
      name: powerScheduleJob
    jdbc:
      initialize-schema: never
      table-prefix: OPT_DASH_BATCH_

